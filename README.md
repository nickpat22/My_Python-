# Custom Filter, Map & Reduce in Python

This project demonstrates the internal working of Python's **filter**, **map**, and **reduce** functions by implementing them manually using loops and lambda functions.

## Features

- Custom `filterX()`
- Custom `mapX()`
- Custom `reduceX()`
- Uses lambda functions
- Beginner-friendly implementation

